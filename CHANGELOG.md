# 更新日志

本文件记录面向玩家的版本变更。逐版本的开发细节、数值近似清单与调参记录见
[`docs/开发进度.md`](docs/开发进度.md)；规则还原基准见 [`docs/官方规则详解.md`](docs/官方规则详解.md)。

版本号同时出现在三处，必须一致：`package.json`、`android/app/build.gradle`
（`versionName` + 递增的 `versionCode`）、git tag。主菜单左下角显示当前版本。

## v0.2.3 — 2026-09-14

**把"只有点进去才发现"的缺陷变成自动能发现的**（不改任何游戏规则与数值）。

### 新增

- **界面冒烟测试（`tests/ui-smoke.test.ts`）**：在 jsdom 里启动真实应用，用真实点击走完
  主菜单 → 图鉴四页 → 玩法说明 → 开局环境三选一 → 备战（买角色/刷新/上阵）→ 出战 →
  战斗 → 结算 → 策略/奖励/补给节点循环 → 通关/结束两屏；每步断言"该屏确实在场"且
  "没有触发渲染兜底"，并断言画布位图尺寸、一场战斗只结算一次、备战期确实写入续档
- 它跑在现有 `npm test` 里，**CI 的发布前门禁自动覆盖**，无需改动工作流

### 为什么值得

上一轮修掉的渲染异常白屏、重生渲染器泄漏、开局盾条不显示等问题，全部属于"只有真的点进去
才能发现"的类型；人工逐屏截图能发现，但**结论不会自动复现**。现在这类缺陷会让冒烟测试失败
并给出具体诊断（例如「渲染触发兜底，应用被重置回主菜单」）。用 jsdom + 桩化 canvas 而不是
Playwright，是为了不引入浏览器下载与额外 CI 时长——项目至今零运行时依赖。

### 更正

- 上一版把"战斗中重绘并存两个渲染器"描述为会导致**双扣血双推进**。本轮实测（去掉守卫后
  中途重绘并打完）**结算次数仍为 1**：结果卡每次重建、DOM 里只存在一个「继续」按钮，
  不存在双结算路径。该修复的实际收益是**消除动画循环泄漏与一次多余的完整模拟**，
  描述已按实测更正（代码修正不变，仍然值得保留）

### 观测

- soak 300 局连跑 5 次：49.3%~57.0%（均值 ≈54%），距断言下沿 40% 有充足余量——上一轮
  记录的单次 47% 属采样噪声。band 断言的失败信息改为直接说明"这是平衡漂移、按
  `references/balance-and-testing.md` 的回退顺序处理"，避免下次误判为状态腐坏

测试 122 → **125 项**（新增冒烟 3 项），全量约 9 秒。

## v0.2.2 — 2026-09-14

深挖轮：三路审计（战斗引擎 / 对局与存档 / 渲染与 UI）后逐条在代码中复核并修正，
每条修正都配了能捕获它的测试（并对修复做变异验证：还原修复必须让对应测试失败）。

### 修复（会改变实战表现的 4 处）

- **团队暴击被算两遍**：编译层与引擎各加一次团队暴击加成，使爆发羁绊（6/14/20%）、
  幸运星、追星之刃、破军星徽、星核猎手 5 档的暴击收益翻倍。现只在引擎施加一次
- **景元「神君」整局只触发一次**：被动层数只设不增，终结技文案写的「神君 +2 层」从未生效。
  现按数据里的 `ultGain` 叠层、封顶 `max`
- **特邀专家·佩拉完全无效**：环境写的团队击杀叠攻从未被引擎读取（只读装备来源）。
  现击杀加成 = 装备 + 团队来源
- **打输最终首领却被判「通关胜利」**：只要走完节点就算通关，输了最后一战照样发职级晋升、
  累计胜场 +1、解锁超频。现末位面末节点失败即判对局结束
- **牌池只减不增**：卖出/卖光策略都不返还牌池（重摇的未购商品却会还）→ 某角色买满后
  永不出现、3★ 不可能。现按 3^(星−1) 统一占用口径，所有增删牌池的路径都过同一对出入口
- **减防无下限**：叠加减防可把防御压到负值，伤害系数变负 → 攻击反而给敌人回血甚至 `Infinity`。
  现防御下限 0（官方式减防上限 100%）
- **四费晋升 + 满备战席会溢出**：合成预判按 1★ 算而晋升实际给 2★，导致备战席变 10 格、
  第 10 个棋子不显示也不可卖

### 修复（存档与可用性）

- **存档脏数据防御补齐**：商店缺字段/含非法角色会让备战界面直接崩成白屏；牌池缺失或
  含 NaN 会让商店永久空；非法站位会让棋子看不见、点不到、卖不掉，却仍占上阵数与羁绊；
  重复 uid 会「卖一个删两个」；计数器里的 NaN 会污染金币（无限白嫖）与战斗（全场必败）；
  经验未按等级归一，一次买经验可直冲满级；字符串 `"false"` 会被当成真值反转超频/锁定
- **选择节点软锁**：空三选一/空奖励的存档会让对局既打不了也推进不了。现载档后按节点
  重进对应阶段（能重摇就重摇，保留选择权）
- **渲染异常白屏**：任何渲染异常此前都会留下空白舞台且没有恢复入口。现捕获后重置当前
  对局并回到主菜单（终身统计保留）
- **战斗中重绘会并存两个渲染器**：战斗中触发重绘（调试钩子/异常重渲染）时旧实例不被销毁，
  它的动画循环对着已卸载的画布一直跑，并白白多跑一次完整的战斗模拟。现进入战斗前先销毁旧
  实例，结算回调也加了幂等守卫。**更正**：早先版本描述为"双扣血双推进"，实测（不带守卫时
  中途重绘并打完）结算次数仍为 1——结果卡每次重建、DOM 里只存在一个「继续」按钮，所以没有
  双结算路径；这条的实际收益是消除泄漏与重复模拟，而非修掉一次双扣血
- **开局护盾不显示**：护盾羁绊/磐盾星徽/现金为王的开战护盾要等该单位首次受击才显示盾条

### 性能

- 战斗静态层（夜空/星点/竞技场地台/透视格线/半场辉光，约占每帧填充像素的 95%）
  改为预渲染缓存，每帧一次 `drawImage`
- 画布位图按实际显示尺寸渲染，舞台缩小时不再按 1440 满分辨率光栅化
- 行动条名字截断结果记忆化、特效数组先判空再过滤

### 内部质量

- 类型：上下文拆分为 `AppCtx` / `GameCtx`，菜单与图鉴在类型层面就读不到对局状态；
  奖励改判别联合；`h()` 泛型化去掉断言
- 同一真值不再两处手写：星级倍率、刷新/买经验的可用性判定、奋斗协议生命代价、
  等级不足提示、利息与商店容量配置（后两者此前是值为错的死配置）
- 清理零引用导出与死分支

平衡（仅测试与渲染改动不改数值，但强度修正会改变胜率）：balance 5 次 50%~70%（均值 ≈60%）、
soak 300 局 47%~57%，均在带内、无需校准——但相比上一版**确实系统性下移约 10pp**，因为两处
修正让难度回到了设计值：团队暴击此前被算两遍（等于白送一档暴击收益），输掉最终首领此前
也算通关。测试 105 → 122 项。

## v0.2.1 — 2026-09-14

质量整治轮：**未改动任何游戏规则与数值**（仅测试、文档、存档防御与战斗演出）。

### 修复

- **战斗飘字不再压住角色名/血条**：飘字改为贴本体出生并按顺序横向扇开，此前固定落在
  头顶上方、上浮时正好穿过上一排单位的角色名与血量数字，实测会把名称糊掉
- **存档脏数据防御补齐**：`hp/gold/level/exp` 与各类计数器遇到 NaN/负数/越界时钳回合法区间
  （此前只校验阶段与节点坐标），并保证上阵数 ≤ 等级、备战席 ≤ 上限
- **诊断测试空转**：`diag` 循环缺少「投资环境」分支，整局空转到步数上限且无断言所以静默通过；
  已补分支与断言
- 存档写入失败（隐私模式/存储配额）此前完全静默，现改为控制台留痕，便于排查"进度莫名丢失"

### 测试

- 测试总数 96 → **105 项**：新增跨文件护栏测试（战斗画布与 CSS 必须 1:1、画布高+顶条=舞台高、
  舞台尺寸与缩放基准一致、费用色 CSS/TS 双轨一致、扣血档位合法），改一边漏另一边会直接失败
- soak 增加**终止分布断言**：区分"正常终局"与"步数上限卡死/未知阶段"，后者必须为 0
- 4 条词缀测试从"弱断言/随机基线比较"改为定序随机精确对照（免死金牌、应激反应、能量逃逸、
  复仇心切），并通过停用词缀效果的变异验证确认会失败

### 维护

- 清理 4 个无引用导出；顾问标记统一走 `advisorsOf`
- 文档与实际状态对齐（八步路线完成、测试项数、三条遗留 P2 逐条结清）
- 菜单显示版本号，取自 `package.json`（单一真源，避免多处手写漂移）

### 已知问题

- 开发依赖 `vitest` 经由 `@vitest/mocker` 带有 2 条 moderate 级公告（任意文件读取），
  仅影响本地测试工具、不进 APK 与网页产物；修复需升级到 vitest 5（破坏性），暂不升级

## v0.2.0 — 2026-09-14

**八步开发路线全部完成。**

- ① 官方商店概率表（Lv1-10）+ 等级上限 10（前台 6 + 后台 4）
- ② 后台角色自动施技 + 后台强度（每角色专属后台赋能）
- ③ 投资策略三选一节点（20 条银/金策略，金策略抬高敌人难度）
- ④ 角色池扩到 24 名并按官方费用校准
- ⑤ 星徽装备 10 枚（穿戴者加入羁绊）+ 装备池扩到 41 + 财富宝钻（后台位 +1）
- ⑥ 行动值倒计时取代敌方行动计数（双方共享预算，随减员动态重算）
- ⑦ 晋升词缀难度 10 条 + 备战情报面板难度值
- ⑧ 投资环境 16 条（含 4 位随行顾问）+ 超频模式（通关解锁的高难开关）+ 对手公司 11 节点 + 概念股刷新加权

## v0.1.8 — 2026-09-14

- 三路审计修复：存档白名单迁移（P0）、装备团队加成/同 id 合成/额外打击事件化/再动不耗行动值/
  风暴骑士系数（P1×5）、渲染同步（P2）
- CI 增加发布前质量门禁（typecheck + test 不通过不出包）；GitHub Actions 升级到 @v5 / Node 22

## v0.1.5 — 2026-09-13

- 版本号三处对齐；出战前防御加固

## v0.1.1 ～ v0.1.4 — 2026-09-13

- 沉浸式全屏 + 星穹铁道风格 UI 起步（v0.1.1）
- 亮色官方风 + 大字号大触控（v0.1.2）
- 同名角色只能上阵一个 + 官方布局战斗界面（v0.1.3）
- 修复顶栏按钮换行与进度条挤占（v0.1.4）

## v0.1.0 — 2026-09-13

- 首个可玩版本：3 位面 17 节点、无头战斗引擎 + Canvas 回放、bot 平衡验证、
  Capacitor 安卓打包与 GitHub Actions 自动出包
